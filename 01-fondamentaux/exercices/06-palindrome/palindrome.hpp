#pragma once
// Signatures imposées de l'exercice 06. NE PAS MODIFIER ce fichier.

#include <string>

std::string reverse(const std::string& s);
bool is_palindrome(const std::string& s);   // insensible à la casse, ignore les espaces ; ASCII uniquement
