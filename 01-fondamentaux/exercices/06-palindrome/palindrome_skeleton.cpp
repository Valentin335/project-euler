// Exercice 06 — Renversement & palindrome.
// Complète le corps des fonctions ci-dessous. Les signatures sont imposées
// par palindrome.hpp : ne les modifie pas.
#include "palindrome.hpp"

std::string reverse(const std::string& s) {
    // TODO: renvoyer la chaine s lue de droite a gauche.
    // Astuce : une boucle range-for ne parcourt que dans le sens normal ;
    // pour inverser, pense a un indice ou a construire le resultat a l'envers.
    return std::string();
}

bool is_palindrome(const std::string& s) {
    // TODO: renvoyer true si s est un palindrome, en ignorant la casse et
    // les espaces (ASCII uniquement). Regarde std::tolower (<cctype>).
    return false;
}
