// =====================================================================
// rotation_skeleton.cpp — À TOI DE JOUER.
// Remplace les corps « bidon » ci-dessous par tes vraies implémentations.
// Rappel : tu reçois des références (int&), donc tes modifications doivent
// se voir chez l'appelant. Interdit : std::swap, std::exchange.
// =====================================================================

#include "rotation.hpp"

void swap_ints(int& a, int& b) {
    // TODO : échanger réellement a et b (par exemple via une variable
    // temporaire). Pour l'instant on ne fait rien : les tests échoueront.
    (void)a;
    (void)b;
}

void rotate_left(int& a, int& b, int& c) {
    // TODO : transformer (a, b, c) en (b, c, a). Pour l'instant on ne fait
    // rien : les tests échoueront.
    (void)a;
    (void)b;
    (void)c;
}
