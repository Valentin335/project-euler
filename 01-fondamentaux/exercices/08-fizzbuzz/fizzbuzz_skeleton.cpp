// Exercice 08 — FizzBuzz parametrable.
// Complete le corps des fonctions ci-dessous. Les signatures sont imposees
// par fizzbuzz.hpp : ne les modifie pas.
#include "fizzbuzz.hpp"

std::string fizzbuzz_term(int n) {
    // TODO: renvoyer "Fizz" si n est multiple de 3, "Buzz" si multiple de 5,
    // "FizzBuzz" si multiple de 15, sinon le nombre n converti en texte.
    // Astuce : l'operateur modulo % donne le reste d'une division ;
    // std::to_string(n) convertit un int en std::string.
    return std::string();
}

std::vector<std::string> fizzbuzz_range(int from, int to) {
    // TODO: construire un vector contenant fizzbuzz_term(i) pour chaque i
    // de from a to inclus. Pense a une boucle et a push_back.
    return {};
}
