#pragma once
// Signatures imposées de l'exercice 08. NE PAS MODIFIER ce fichier.

#include <string>
#include <vector>

std::string fizzbuzz_term(int n);                          // "Fizz" (multiple de 3), "Buzz" (multiple de 5), "FizzBuzz" (multiple de 15), sinon le nombre en texte
std::vector<std::string> fizzbuzz_range(int from, int to); // termes de from a to inclus
