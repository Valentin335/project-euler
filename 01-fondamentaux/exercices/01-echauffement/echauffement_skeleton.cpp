#include "echauffement.hpp"

int square(int x) {
    /* x → x*x */
    (void)x;
    return 0;
}

long long sum_to(int n) {
    /* n → 1+2+...+n (en 64 bits) */
    (void)n;
    return 0;
}

bool is_even(int x) {
    /* x → vrai si pair */
    (void)x;
    return false;
}
