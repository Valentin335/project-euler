#include "split.hpp"

std::vector<std::string> split(const std::string& s, char sep) {
    /* (s, sep) → champs séparés par sep, vides conservés ; "" → {""} */
    (void)s;
    (void)sep;
    return {};
}
