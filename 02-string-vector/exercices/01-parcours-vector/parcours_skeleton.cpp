#include "parcours.hpp"

int somme(const std::vector<int>& v) {
    /* v -> somme des éléments (0 si vide) */
    (void)v;
    return 0;
}

void mettre_au_carre(std::vector<int>& v) {
    /* chaque élément -> son carré, en place */
    (void)v;
}

int compte_pairs(const std::vector<int>& v) {
    /* v -> nombre d'éléments pairs */
    (void)v;
    return 0;
}

int max_value(const std::vector<int>& v) {
    /* v -> plus grand élément ; throw std::invalid_argument si vide */
    (void)v;
    return 0;
}
