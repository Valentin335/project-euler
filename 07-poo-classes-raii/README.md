# Chapitre 7 — POO : classes, RAII, règle de 0/3/5 (en construction)

> Ce chapitre n'est pas encore développé. Il contient pour l'instant **un seul
> exercice en attente**, déplacé depuis le chapitre 1 parce qu'il demande
> d'écrire un **destructeur** — une notion de classes qui n'a sa place qu'ici,
> après le cours sur la POO.

## Exercice en attente

- `exercices/raii-scoped/` — `ScopedAppender` : une classe dont le **destructeur**
  garantit une action de fin de vie (le pattern RAII). Déplacé du ch.1 : on ne
  code pas un destructeur avant d'avoir vu les classes.

Le reste du chapitre (constructeurs, encapsulation, surcharge d'opérateurs,
règle de 0/3/5, projet `Rational`/`Matrix`) sera construit le moment venu.
