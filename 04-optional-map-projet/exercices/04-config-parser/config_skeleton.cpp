#include "config.hpp"

std::map<std::string, std::string> parse_config(const std::vector<std::string>& lignes) {
    /* lignes "cle=valeur" -> map cle->valeur (ligne sans '=' ignorée) */
    (void)lignes;
    return {};
}
